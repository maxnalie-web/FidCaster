- [LSP language routing](lsp-language-routing.md) — python→pyright-langserver, ts/js→typescript-language-server, css/scss/less→vscode-css-language-server, html→vscode-html-language-server, json/jsonc→vscode-json-language-server, markdown/mdx→vscode-markdown-language-server. All in api-server/node_modules/.bin.
- [LSP Monaco provider pattern](lsp-monaco-pattern.md) — Register in effect with deps [monaco, projectId, language] only. Providers read model.uri.path at call time. Separate effect for didOpen (deps: path, projectId, language). Signature help registered with triggerChars ["(", ","]. Providers cached in _lspProviders.
- [Debugger source mapping](debugger-source-mapping.md) — debug/start returns tmpDir. CDP: Debugger.scriptParsed builds scriptId→URL map; Debugger.paused strips tmpDir prefix. Breakpoints use file:///tmpDir/file. DAP: setBreakpoints uses tmpDir/file; stackTrace strips tmpDir from source.path.
- [Notebook key remount](notebook-remount.md) — NotebookView must have key={activeTabId} to prevent cell state leaking between .ipynb files.
- [Notebook rich outputs](notebook-rich-outputs.md) — notebook_executor.py uses IPython InteractiveShell, JSON-lines stdio. Returns display_data/execute_result MIME bundles. API returns `outputs` (rich) + `output` (legacy text).
- [LSP WebSocket proxy block](lsp-ws-proxy.md) — lsp.ts isLocalRequest() always returns false in Replit because proxy sets X-Forwarded-For. Fix: remove isLocalRequest guard from WSS upgrade handler entirely.
- [Localhost security guard](localhost-security.md) — requireLocalhost checks !isLocal OR hasProxy (X-Forwarded-For). Applied to debug.ts and notebook.ts. IP-only check bypassable via reverse proxy.
- [DevStation env paths](devstation-env.md) — Python at .pythonlibs/bin/python3; LSP binaries in api-server/node_modules/.bin; IPython/ipykernel installed in .pythonlibs.
- [Multi-AI provider agent](multi-ai-provider-agent.md) — Two separate loops: runAnthropicLoop (native SDK + stream.on("text")) and runOpenAILoop (OpenAI SDK stream, accumulates tool_calls across chunks). Route by provider in /agent/run. GET /agent/models returns all models with available flag. UI model selector in agent-panel with localStorage persistence.
- [Claude 4.x prompt caching](claude4-caching-break.md) — cache_control on SYSTEM BLOCK + beta header breaks claude-4.x. Workaround: inject systemPrompt as first content block of first USER message with cache_control:ephemeral; set system param to minimal string. Beta header is safe when not on system block.
- [Agent cost reduction strategy](agent-cost-reduction.md) — System prompt must be STATIC (no dynamic content) for cache to hit across runs. Dynamic context (secrets, stack, project structure) goes in buildAutoContext → auto_context user message block. Tool results truncated to 2500 chars in context. Old turns compressed (compressOldToolResults). Chat history limit: 10 not 30. Parallel tool calls mandatory in prompts.
- [Anthropic model roster 2026](anthropic-models-2026.md) — Current models (June 2026): fast=claude-haiku-4-5-20251001, balanced=claude-sonnet-4-6, powerful=claude-opus-4-8. Sonnet 4.6+ does NOT support prefill. Haiku/Sonnet support extended thinking; Opus 4.8 uses adaptive thinking (always on). max_tokens 8192 for agent loops.
- [Agent provider selection priority](agent-provider-priority.md) — claudeKey availability must be checked BEFORE isPro. If Claude_API set, all users get Claude (free=haiku, pro=selected). Never send non-pro to Gemini when Claude key exists. throw loopErr re-throws to outer catch hiding errors — use sseWrite directly instead.
- [Gemini OpenAI-compat URL](gemini-url-fix.md) — streamOpenAICompat must use base.replace(/\/$/, "")+"/chat/completions" NOT base+"/v1/chat/completions". Gemini base: "https://generativelanguage.googleapis.com/v1beta/openai". Use gemini-2.0-flash not gemini-1.5-pro (deprecated in v1beta OpenAI compat).
- [AI panel always present](ai-panel-present.md) — AgentPanel MUST remain in desktop (left 20%), tablet (left 220px), and mobile nav. User never wanted it fully removed. Shell/Console are tool tabs only (no bottom panel split). node-pty required for real PTY shell.
- [Power Tools panels](power-tools-panels.md) — API Tester, Todo Board, Snippets, Time Tracker panels added. RightPanel union type + toolbar buttons pattern for adding new IDE panels.
- [Playwright E2E in Replit NixOS](playwright-replit.md) — Use Nix system chromium (executablePath: `which chromium`). Browser hangs in agent bash sandbox; works in CI. E2E pkg at artifacts/e2e, not packages/e2e.
- [DevStation design system](devstation-design.md) — Amber/emerald terminal aesthetic. BG #0A0A0A, surface #111111, border #1C1C1C, primary amber #F59E0B, secondary emerald #10B981. DevLogo in src/components/dev-logo.tsx. All THEME_VARS use amber as --primary.
- [React Query key mismatch bug](react-query-key-mismatch.md) — useListFiles custom queryKey overrides generated key; invalidateQueries uses generated key → no-op. Never pass custom queryKey to generated hooks unless all invalidations also use that same key.
- [Playwright .or() strict mode](playwright-or-strict.md) — .or() creates a union locator; if all alternatives exist simultaneously, toBeVisible() throws strict mode violation. Fix: add .first() to the outer chain, or use {exact:true} to narrow matches.
- [Terminal xterm.js testing](terminal-xterm-testing.md) — expose window.__terminalSend in ws.onopen (DEV only); use waitForFunction to confirm ready; send \n not \r (no PTY = bash expects LF). ls output on Replit HOME is "workspace".
- [aiClient API key env var](aiclient-api-key.md) — aiClient.ts getDefaultAnthropicKey() must check Claude_API first, then ANTHROPIC_API_KEY, then AI_INTEGRATIONS_ANTHROPIC_API_KEY. Missing Claude_API caused silent SSE error on /api/ai/chat.
- [Chat history per project](chat-history-schema.md) — chatMessagesTable in lib/db/src/schema/chat-history.ts. GET/POST/DELETE /api/projects/:id/chat-history in ai.ts. agent-panel loads on mount (useEffect[projectId]), saves after each exchange. Table created via ensureChatHistoryTable() raw SQL on server startup.
- [Agent architecture optimizations](agent-optimizations.md) — TTLCache in lib/cache.ts (provider 30s, files 10s, index 60s). Prompt caching via user-message content block (see claude4-caching-break.md). Dynamic knowledge selection (knowledge-selector.ts). buildRepoOverview lists file names+sizes only (no buildProjectIndex). RAG: queryRelevantFiles() prepended to first user message. trimMessages/trimOpenAIMessages validate tool_use/tool_result pairs.
- [pnpm store restoration](pnpm-store-restoration.md) — When pnpm store is partially empty, run pnpm install --prefer-offline --ignore-scripts twice: first pass downloads missing packages, second reuses cache and completes in seconds. node-pty has no Linux prebuilds; use try/catch require() for graceful fallback.
- [Auth implementation](auth-implementation.md) — JWT+scrypt via Node.js built-in crypto. Cookie: ds_token (httpOnly, sameSite:lax). requireAuth middleware in middlewares/require-auth.ts. Routes: POST /api/auth/register, POST /api/auth/login, POST /api/auth/logout, GET /api/auth/me. Projects filtered by userId. useAuth() hook + AuthGuard component in frontend.
- [WebSocket unmount reconnect race](ws-unmount-race.md) — ws.onclose fires after React cleanup → schedules new reconnect on unmounted component. Fix: unmountedRef.current=true in cleanup, guard all reconnect schedules with !unmountedRef.current.
- [drizzle-kit push TTY requirement](drizzle-push-tty.md) — drizzle-kit push prompts user to confirm destructive changes (UNIQUE constraints on non-empty tables). Fails with "Interactive prompts require TTY" in CI/non-interactive shells. Fix: pass --force flag in post-merge.sh. PostgreSQL ADD CONSTRAINT IF NOT EXISTS is not valid syntax; use DO $$ BEGIN IF NOT EXISTS ... END $$;
- [Agent bash-file sync gap](agent-bash-sync.md) — bash tool writes to tmpdir but only str_replace_editor called syncFileFromDir. Fix: syncNewFilesFromDir after every bash call + in agent loop end + finally block. MAX_ITERATIONS was 8 (too low for real tasks); raised to 25.
- [Agent panel duplicate message](agent-panel-duplicate.md) — finally block in startAgent called setChatMessages AND agentMessage state both rendered same text. Fix: remove setChatMessages from finally — only saveChatMessage (DB) + agentMessage (display).
- [Chat bubble styling](chat-bubble-styling.md) — User bubble: bg-primary/15 border-primary/20 text-gray-900. Agent reply: NO box, plain py-0.5 div + text-gray-900. RunResultCard content: no box either (isFailed gets red box). Never use text-white/90 on dark bg for chat text.
- [Project-scoped DB routes](project-db-routes.md) — Added GET /projects/:id/db/tables, /db/schema, /db/rows, POST /db/query to projects.ts. Uses _getProjectDbUrl() helper that fetches DATABASE_URL from secretsTable. PgPool cached per connection string. DatabasePanel auto-connects on mount when status=active.
- [Agent tool label system](agent-tool-labels.md) — getToolLabel(tool, input) replaces old getToolSummary. Returns human-readable text per tool (e.g. str_replace_editor+view → "Opened foo.tsx"). Registered in agent-panel.tsx. BRAIN_TOOLS set controls Brain vs Code2 icon.
- [IDE panel order](ide-panel-order.md) — PanelGroup order: Agent(order=1) | Editor(order=2) | Library(order=3, desktop only). Library is RIGHT of editor with border-l. Resize handle after editor, before library. Editor order is always 2 (tablet and desktop).
- [Profile fields and stack detection](profile-and-stack.md) — Profile has: bio, avatar_color, github_url, website_url, location, display_name, twitter_url, timezone (all via ensureProfileColumns() raw SQL at startup). AuthUser interface in use-auth.ts includes all extended fields. Agent detects stack (pnpm/yarn/npm, runtime, Vite/Next/etc, TS, Tailwind, Drizzle) from project file paths → detectedStack in AgentPromptContext → injected into <project_context> block.
- [DB startup migrations pattern](db-startup-migrations.md) — All column additions go in index.ts db.execute() block as `ALTER TABLE X ADD COLUMN IF NOT EXISTS`. Projects table needs color TEXT, pinned BOOLEAN DEFAULT FALSE, last_opened_at TIMESTAMPTZ — schema and actual DB can diverge if startup migration wasn't present when table was created.
- [Modal theming rule](modal-theming.md) — Never use theme-dark class or hardcoded #0d1117 in modal components. Use bg-card text-card-foreground on outer wrapper. Replace bg-white/N → bg-muted/N, text-white → text-foreground, text-white/50 → text-muted-foreground, border-white/N → border-border. CSS variables adapt to all themes automatically.
- [Agent tool nudge on early exit](agent-tool-nudge.md) — Anthropic: tool_choice:{type:"any"} for iterations≤3; nudge up to iteration 8 (hard-reset conversation). OpenAI/Gemini: tool_choice:"auto"; nudge≤8 also hard-resets to [originalTask, assistantText, directive]. detectTaskComplexity handles Persian/Arabic (بساز etc.) and non-ASCII. max_tokens=8192.
- [Agent quality fixes](agent-quality-fixes.md) — (1) create guard: if file exists, return error forcing str_replace. (2) Completion gate: hasRegisteredWorkflow && !hasTestedApp → inject verify nudge (max 2x). (3) Removed background setImmediate compression (was making silent 18K-token calls not counted in UI). (4) Prompt: TypeScript React = ONLY .tsx/.ts, NEVER .jsx/.js — mixed extensions break Vite.
- [Free-tier agent config](free-tier-agent.md) — Free tier: tries AI_INTEGRATIONS_ANTHROPIC_API_KEY (Replit Claude) first → claude-haiku-4-5; falls back to Gemini_API_Key_FREE → gemini-2.5-flash. isGeminiEndpoint → tool_choice:"auto". Replit integration requires phone-verified account + AI_INTEGRATIONS_ANTHROPIC_BASE_URL set (via setupReplitAIIntegrations). Key is dummy string; base URL is what routes to proxy.
- [Agent power tools set](agent-power-tools.md) — 7 new tools added: web_search (DuckDuckGo HTML scraping), read_url (HTML→text), memory_tool (read/write AGENT_MEMORY.md per project), run_tests (auto-detect vitest/jest/pytest/cargo), git_diff, git_commit, lint_check (tsc+ESLint). All in agentTools.ts + agent.ts executeAgentTool switch.
- [Bash streaming SSE](bash-streaming-sse.md) — runBash accepts optional onChunk callback; flushes every 1024 chars or 500ms. executeAgentTool bash case passes onChunk→sseWrite(bash_chunk). Frontend: ToolCall.liveOutput accumulates chunks, shown as terminal box under running chip; cleared on tool_done.
- [Parallel tool executor](parallel-executor.md) — lib/parallel-executor.ts. Pure-read tools (repo_overview, search, web_search, read_url, str_replace_editor/view) run concurrently. Writes on distinct paths also parallel. bash/run_command/git/DB always sequential. Per-run readCache deduplicates same-file reads.
- [Agent auto-context injection](agent-auto-context.md) — buildAutoContext() reads AGENT_MEMORY.md, package.json (deps+scripts), README/replit.md, and project file list from DB. Injected as XML block appended to first user message in both runAnthropicLoop and runOpenAILoop. Graceful: all reads in try/catch.
- [Chat steps persistence](chat-steps-persistence.md) — chat_messages.steps TEXT column stores JSON-serialized ToolCall[]. role enum expanded to include run_result. finally block saves saveChatMessage("run_result", msg, capturedCalls). Load history restores run_result+toolCalls. DB migration: ALTER TABLE ADD COLUMN IF NOT EXISTS + DO $$ DROP/ADD CONSTRAINT $$.
- [Agent tool call stale ref](agent-tool-stale-ref.md) — toolCallsRef updated via useEffect (async, lags by one render). Use toolCallsAccumRef updated SYNCHRONOUSLY in event handlers (tool_start/done/bash_chunk). capturedCalls must read from accumRef, not toolCallsRef.
- [RunResult steps JSON format](run-result-steps-format.md) — steps column now stores {toolCalls: ToolCall[], runStats: {...}} object (not plain array). Load code handles both: array → old format; object with toolCalls key → new format. runStats persists across refresh.
- [Preview port baseline exclusion](preview-port-baseline.md) — PreviewPanel probes on mount to record baseline ports (IDE/API server). Background 5s poll excludes these; only NEW ports trigger app mode. IDE now on 5173, so baseline includes 5173.
- [IDE artifact port 5173](ide-port.md) — IDE artifact runs on port 5173 (was 5000). Replit platform permanently binds 5000 at OS level; app cannot bind it. artifact.toml localPort=5173, env PORT="5173". configuredPort prop on PreviewPanel passes ?extraPort= to /running-port.
- [Free subdomain deploy system](free-subdomain-deploy.md) — Projects run as child processes (ports 4000-4999) inside DevStation's own Replit deployment. Express proxy intercepts Host: slug.devstation.site. Deployments migration must be AWAITED before auto-restart code runs. DNS: *.devstation.site CNAME → deployed Replit app (Cloudflare).
- [HTML run config](html-run-config.md) — HTML language was missing from LANG_CONFIGS in run.ts. Added: checks package.json scripts, then server.js/app.js (node), server.ts (tsx), else python3 -m http.server 3000. "Could not find run command" user complaint = Railway saying this when deploy.ts creates empty service with no code pushed.
- [Plan mode user bubble timing](plan-mode-bubble.md) — In plan mode handleSubmit, add user bubble to chatMessages + save to DB BEFORE fetchPlan. Set skipNextUserBubble.current=true so startAgent (called from handleConfirmPlan) doesn't duplicate it.
- [Sequence reset empty-table guard](sequence-reset.md) — setval(seq, 0) crashes (out of bounds). Use GREATEST(COALESCE(MAX(id), 1), 1) so empty tables default to 1 not 0.
- [Mobile artifact collab import](mobile-collab-import.md) — collab.ts must NOT import `db` from ../index.ts (index.ts exports nothing). Remove DB import; use inline logic or pass db as parameter.
- [Yjs collab architecture](yjs-collab.md) — y-websocket v3 is browser-only (no server setupWSConnection). Server = dumb relay in app.ts on /ws/yjs/:id via yjsWss. Client: useYjsCollab hook (use-yjs-collab.ts) creates Y.Doc+WebsocketProvider per project; getYjsText(path) returns Y.Text per file. MonacoBinding created in useEffect[editorMounted,path,yjsDoc]. setEditorMounted(true) called inside handleMount.
- [Admin logs route](admin-logs-route.md) — artifacts/api-server/src/routes/admin-logs.ts. GET /api/admin/logs (file-based JSON lines), GET /api/admin/logs/chat (DB chat_messages for admin users), DELETE /api/admin/logs. Checks ADMIN_EMAILS env var. writeAdminLog() exported for use in agent.ts. Registered in index.ts after tiersRouter.
- [Chat history injected into agent](chat-history-injection.md) — loadChatHistory(projectId, limit=30) in agent.ts queries chatMessagesTable ordered by createdAt asc. Maps user→user, assistant/run_result→assistant (merging consecutive). Ensures starts with user message. Passed as priorMessages to both runAnthropicLoop and runOpenAILoop. Prepended before enrichedPrompt.
- [Agent message trim validation](agent-trim-validation.md) — trimMessages() and trimOpenAIMessages() replace the broken sliding window. Validate tool_use/tool_result pairing: never start with tool_result, drop orphan tool_results. Called after every message push in both loops.
- [Dynamic knowledge selector](dynamic-knowledge-selector.md) — knowledge-selector.ts parses AGENT_KNOWLEDGE by ### headers, scores sections by keyword match against userPrompt. Replaces always-injecting the full 60KB. userPrompt field added to AgentPromptContext; passed from /agent/run route.
- [Agent timeline UI](agent-timeline-ui.md) — TimelineItem union type + TimelineItemRow component in agent-panel.tsx (module level). timeline state (TimelineItem[]) replaces ToolCallRow list in running view. Updated in handleAgentEvent: agent_thinking→push thinking, tool_start→push tool, tool_done→update status+duration, agent_message→push message, agent_done→push done, agent_error→push error. setTimeline([]) in startAgent reset. RunResultCard unchanged (still uses toolCalls from DB).
- [FidCaster wallet session persistence](fidcaster-wallet-session.md) — Wallet auth sessions saved to sessionStorage (fc_wlt_v1); restored on refresh without new signMessage via setWalletAccountChangeCallback. loginWithWallet callback MUST check address equality: same addr → update walletClient only; different addr → full re-login. Missing check causes signerApproved:false on every page nav → faint likes/recasts.
- [FidCaster signer architecture](fidcaster-signer.md) — Signer MUST be on-chain via Farcaster KeyRegistry (Optimism). NEVER use Neynar Managed Signer. User explicitly rejected Neynar approach.
- [FidCaster FID Market list() ABI](fidcaster-fidmarket-list-abi.md) — Real ABI: list(fid, priceWei, durationSecs[600-2592000], fromDeadline[≤bt+dur], fromSig). 3rd arg=durationSecs NOT nonce. sig NOT verified at list time (stored for buy). Contract: 0xcc11C0Bc08bbF8A5C0AAca80E884C6c7CC0eE3c3 Optimism.
- [FidCaster FID Market ABI](fidcaster-market-abi.md) — Correct ABI for 0xcc11C0Bc08bbF8A5C0AAca80E884C6c7CC0eE3c3 on Optimism: cancel(uint256); list(fid,priceWei,durationSecs,fromDeadline,fromSig) — 3rd arg=durationSecs NOT nonce; buy(fid,seller,toDeadline,toSig) — fromSig stored on-chain.
- [FidCaster server-side hub submission](fidcaster-hub-server.md) — Write actions (like/recast/follow/cast) go through Express server on port 3001 using @farcaster/hub-nodejs. Vite proxies /api/farcaster/ → localhost:3001. hub-submit.ts in browser is now a thin fetch wrapper. Neynar hub (hub-api.neynar.com) has our key indexed; Pinata does not. Ed25519 signer derived at m/44'/60'/0'/0/1 with ed25519.getPublicKey (NOT m/44'/461'). Read-only Neynar API calls untouched.
- [FidCaster hub "fid cannot be 0" meaning](fidcaster-hub-fid-zero.md) — Hub error "fid cannot be 0" = "unknown signer → FID resolved to 0". Does NOT mean encoding bug. Fix: signer key must be registered in KeyRegistry on Optimism. WalletProvider had dangerous cache fallback — removed.
- [FidCaster security hardening](fidcaster-security.md) — Express server: helmet + CORS restricted to Replit/localhost origins + rate limiting (global 100/min, action 200/min, market read 120/min, market write 20/min) + body limit 10kb + listen on 127.0.0.1 not 0.0.0.0. Input validation: signerPrivateKey = 64-char hex, action.type allowlist, castHash regex, FID bounds (1–1B), ETH address regex, rawTx hex. CSP in index.html meta tags. ws overridden ≥8.21.0 (DoS CVE-2026-48779). Removed .backup/ SQL file with exposed secret. Seed phrase stored encrypted (AES-GCM-256 + PBKDF2-200k) in IndexedDB via session-crypto.ts — never in localStorage.
- [FidCaster session restore bugs](fidcaster-session-restore.md) — hasStoredSession() was only checking SESSION_KEY (old format), not account_${fid} (new format) → every refresh appeared locked. actionLimiter was max:20 → batch follow failed at exactly 20. doLock/onVisibility were calling clearLightSession() → profile lost after lock, bounce to landing page.
- [FidCaster FID Market integration](fidcaster-fid-market.md) — Market tab at /market and /market/:id. Server: server/fid-market-routes.ts (viem indexer, in-memory cache). Hook: useMarketWallet (window.ethereum, no wagmi). Vite proxy: /api/fid-market → localhost:3001. Optimism RPC max block range = 5000 (LOG_CHUNK_SIZE). Market nav in sidebar + bottom nav via navigate("/market").
- [FidCaster FID Market cancelled-filter bug](fidcaster-market-cancelled-filter.md) — Never filter trackedFids by Cancelled events. A FID can be list→cancel→relist; filtering by Cancelled event silently removes active listings. Always use readFidListing() (on-chain state) to determine active status.
- [FidCaster FID Market activity feed](fidcaster-market-activity.md) — Activity feed combines Listed+Sold+Cancelled events with type tags. Real sales require: priceWei>0 AND buyer!=="" (empty buyer means event is not a real sale — the auto-detected Bought topic may match non-sale events from the same contract). tradesCache filters both. GET /api/fid-market/activity returns unified activityCache rebuilt after every refresh.
- [FidCaster SIWF relay endpoint](fidcaster-siwf-relay.md) — Correct relay: POST https://relay.farcaster.xyz/v1/channel (NOT /v1/channel/open). Body: {siweUri, domain}. SIWF always gives read-only; posting needs seed phrase or connected wallet. registerSignerOnchain wraps simulateContract in try/catch — on failure falls back to direct writeContract with chain:optimism so wallet does own simulation.
